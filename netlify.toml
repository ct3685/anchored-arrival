[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Next.js on Netlify uses the automatic runtime adapter
# No publish directory or redirects needed - handled automatically
